[project]
name = "preppilot-api"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "groq",
  "pypdf",
  "python-multipart",
  "python-dotenv"
]

[tool.vercel]
entrypoint = "main:app"